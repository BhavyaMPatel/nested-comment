CREATE TABLE comment
(
    id         INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    comment_by INTEGER,
    comment_on INTEGER,
    comment    TEXT,
    CONSTRAINT pk_comment PRIMARY KEY (id)
);

CREATE TABLE post
(
    id         INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    created_by INTEGER,
    created_at TIMESTAMP WITHOUT TIME ZONE,
    CONSTRAINT pk_post PRIMARY KEY (id)
);

CREATE TABLE "user"
(
    id       INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    username VARCHAR(20),
    CONSTRAINT pk_user PRIMARY KEY (id)
);

ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_COMMENT_BY FOREIGN KEY (comment_by) REFERENCES "user" (id);

ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_COMMENT_ON FOREIGN KEY (comment_on) REFERENCES comment (id);

ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_COMMENT_ONjklo9E FOREIGN KEY (comment_on) REFERENCES post (id);

ALTER TABLE post
    ADD CONSTRAINT FK_POST_ON_CREATED_BY FOREIGN KEY (created_by) REFERENCES "user" (id);
CREATE TABLE comment
(
    id         INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    comment_by INTEGER,
    comment_on INTEGER,
    comment    TEXT,
    CONSTRAINT pk_comment PRIMARY KEY (id)
);

CREATE TABLE post
(
    id         INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    created_by INTEGER,
    created_at TIMESTAMP WITHOUT TIME ZONE,
    CONSTRAINT pk_post PRIMARY KEY (id)
);

CREATE TABLE "user"
(
    id       INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    username VARCHAR(20),
    CONSTRAINT pk_user PRIMARY KEY (id)
);

ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_COMMENT_BY FOREIGN KEY (comment_by) REFERENCES "user" (id);

ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_COMMENT_ON FOREIGN KEY (comment_on) REFERENCES comment (id);

ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_COMMENT_ONzWiQhM FOREIGN KEY (comment_on) REFERENCES post (id);

ALTER TABLE post
    ADD CONSTRAINT FK_POST_ON_CREATED_BY FOREIGN KEY (created_by) REFERENCES "user" (id);